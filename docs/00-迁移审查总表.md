# 知简重构迁移审查总表

- 文档状态：当前实现基线
- 适用范围：`zhijian` 到 `zhijian-home` 的渐进迁移
- 最后核对：2026-07-28
- 维护范围：迁移路线、迁移单元状态、关键决定与审查记录
- 关联代码：根目录工程配置、`src/`、`public/`

> 本文档是 `zhijian` 向 `zhijian-home` 迁移期间的唯一进度基线。
> 迁移不是复制旧代码，而是逐项确认需求、审查旧实现、决定保留或重写、完成新实现，并由人工审查通过。

本文只管理迁移范围、状态、决定和验证证据，不替代后续按需建立的产品、架构、接口或开发规范文档。

## 1. 项目与目标

| 项目 | 路径 | 角色 |
| --- | --- | --- |
| 旧项目 | `../zhijian` | 功能和行为参考，不视为可信实现 |
| 新项目 | `.` | 重构迁移后的正式项目 |

以上路径均以新项目仓库根目录为基准，两个项目保持在同一父目录下。

本次迁移有三个同时进行的目标：

1. **迁移**：保留经过确认的产品能力和必要数据。
2. **重构**：重新建立清晰的目录、依赖边界、类型和样式体系。
3. **审查**：逐项检查旧代码的正确性、安全性、性能、可访问性和可维护性，不默认旧实现正确。

## 2. 强制执行规则

### 2.1 每次只迁移一个可审查单元

一个迁移单元应满足：

- 通常只覆盖一个页面、一个用户流程，或一组不可拆分的基础设施。
- 相关改动可以独立构建、验证和回退。
- 提交前能够用几分钟说明清楚旧行为、发现的问题和新实现。
- 不把多个互不相关的页面或 API 混在同一次迁移中。

### 2.2 “已实现”不等于“已通过”

进度必须同时记录两个状态：

| 字段 | 可选值 | 含义 |
| --- | --- | --- |
| 实现状态 | `未开始` / `分析中` / `实现中` / `已实现` / `不迁移` | 代码工作进度 |
| 人工审查 | `未审查` / `审查中` / `需修改` / `已通过` / `不适用` | 用户的审核结论 |

只有同时满足以下条件，迁移单元才算真正完成：

- 实现状态为 `已实现` 或经确认的 `不迁移`；
- 人工审查状态为 `已通过`；
- 自动检查通过；
- 本文档已记录结论和对应提交。

AI 完成代码后只能把条目标记为 `已实现 / 未审查`，不得代替用户标记 `已通过`。

### 2.3 不直接复制未经审查的旧代码

每个迁移单元开始前必须先回答：

1. 旧功能实际解决什么问题？
2. 该功能是否仍然需要？
3. 旧实现有哪些正确性、安全、性能、体验或结构问题？
4. 哪些内容可以保留，哪些需要重写，哪些应删除？
5. 新实现的验收标准是什么？

发现旧代码问题时，先把问题和处理决定记录到本文件或该单元的审查记录，再写新实现。

### 2.4 Git 与提交边界

- 原则上一个迁移单元对应一个 Conventional Commit；确需拆分时，提交仍须围绕同一单元。
- 不在迁移提交中顺手修改无关模块。
- 每个条目完成实现后记录提交哈希。
- 人工审查要求修改时，在同一条目的“审查记录”中追加结论，不覆盖历史结论。

## 3. 当前基线

统计核对日期：2026-07-28。

统计范围包含已提交的导航页；不含工作区中尚未提交的改动。

### 3.1 新旧项目规模

| 指标 | 旧项目 | 新项目 | 说明 |
| --- | ---: | ---: | --- |
| 页面路由 | 18 | 4 | 新项目已有首页 `/`、分页博客列表 `/blog`、文章详情 `/blog/[slug]` 与导航页 `/nav`；首页已接入首屏、个人信息、最新文章和开源项目区 |
| API 路由 | 34 | 3 | 已提供分类标签筛选 `/api/blog/filters`、分页文章 `/api/posts` 与固定三篇最新文章 `/api/posts/latest` |
| React 组件 | 80 | 29 | 已有站点头部、页脚、首页区块、博客列表、详情、筛选、导航组件及基础 UI 组件 |
| CSS Modules | 84 | 32 | 全局加载态、公开壳层、站点头部、页脚、首页区块、博客列表、详情、筛选与导航组件均使用就近样式 |
| `lib/core` 文件 | 16 | 6 | 已迁移 MySQL 连接池、统一 API 响应、错误处理、限流、客户端 IP 提取和日期展示方法 |
| `lib/domain` 文件 | 18 | 2 | 已迁移首页最新文章与分页文章列表的只读查询；分类和标签筛选逻辑由文章领域层统一处理 |
| `public` 静态资源 | 40 | 37 | 已包含首页基础素材、favicon 资源组、默认 OG 分享图、列表水墨背景与迁移后的文章封面 |
| `src` 文件总数 | 278 | 100 | 新项目已接入公开路由、顶部导航、页脚、首页区块、博客列表、详情、分类标签筛选、导航页与公开接口 |

### 3.2 已存在的新项目基础

| 编号 | 迁移单元 | 当前内容 | 实现状态 | 人工审查 | 提交 |
| --- | --- | --- | --- | --- | --- |
| B-001 | 项目骨架 | Next.js 16、React 19、TypeScript、ESLint、`src/app` | 已实现 | 已通过 | `35c8aa6`、`3559805`、`cd66c20` |
| B-002 | 设计基础 | 设计令牌、字体、亮暗主题与语义色 | 已实现 | 已通过 | `642c215`、`5e7624a`、`4068613` |
| B-003 | 基础状态 | 全局加载态、共享布局类型 | 已实现 | 已通过 | `5e7624a`、`8943502`、`09ec5a6`、`f9c12f3` |
| B-004 | 首页素材 | Logo、水墨背景、独立头像与 favicon 资源组 | 已实现 | 已通过 | `d0e216e`、`6d72e5c` |
| B-005 | 迁移审查机制 | 本文档及逐单元审查流程 | 已实现 | 已通过 | `cd66c20`、`c6c0287` |
| B-006 | 工程完成标准 | 统一检查命令与测试策略 | 已实现 | 已通过 | `0ab07aa` |
| B-007 | 环境与配置基线 | Next.js 类型生成、环境变量与配置边界 | 已实现 | 已通过 | `8c5e646` |

截至统计核对日期，现有代码已通过：

- ESLint；
- TypeScript `tsc --noEmit`；
- Next.js 生产构建。

当前 `npm audit --omit=dev` 报告 Next.js 16.2.11 依赖链中的 3 个高危风险：PostCSS 尚无直接修复，sharp 提示可通过 `npm audit fix` 处理。依赖升级需要作为独立单元复查，不在首页迁移提交中执行。

当前生产构建包含 `/`、`/blog`、`/blog/[slug]`、`/nav`、`/_not-found`，以及动态 `/api/blog/filters`、`/api/posts` 和 `/api/posts/latest`。首页采用 60 秒 ISR，已发布文章查询使用同周期服务端缓存；首页与文章详情已接入 canonical、Open Graph、Twitter Card 及 JSON-LD；公开接口统一返回 `{ code, message, data }`、客户端 IP 提取与限流，首页不再迁移 GitHub 提交热力图。

## 4. 总体迁移路线

迁移顺序以“尽早形成一个可以人工验收的完整切片”为原则。后续可以根据审查结果调整顺序，但调整必须记录原因。

| 阶段 | 范围 | 当前状态 | 完成标准 |
| --- | --- | --- | --- |
| P0 | 仓库重建与审查机制 | 已完成 | B-001 至 B-007 全部人工通过 |
| P1 | 首页静态体验 | 已完成 | 首页视觉、响应式、基础交互可独立验收 |
| P2 | 公开站点壳层与博客读取链路 | 进行中 | 首页、博客列表、文章详情形成只读闭环 |
| P3 | 认证与后台壳层 | 未开始 | 管理员可安全登录并进入后台 |
| P4 | 内容管理 | 未开始 | 文章、分类、标签和图片管理闭环可用 |
| P5 | 用户与导航站 | 未开始 | 游客本地使用和登录用户同步均可验收 |
| P6 | 网站统计“观澜” | 未开始 | 采集、聚合、查询和后台展示形成闭环 |
| P7 | SEO、兼容、部署与发布 | 未开始 | 生产配置、迁移数据和回归检查完成 |

## 5. 详细迁移清单

### 5.1 P0：仓库重建与审查机制

| 编号 | 单元 | 旧项目参考 | 新项目目标 | 实现状态 | 人工审查 |
| --- | --- | --- | --- | --- | --- |
| B-001 | 项目骨架 | 根目录工程配置 | 审查版本、脚本、目录、TypeScript 和 ESLint 策略 | 已实现 | 已通过 |
| B-002 | 设计令牌与主题 | `src/app/tokens.css`、`theme.css` 及旧样式体系 | 确认令牌命名、颜色、暗色模式和浏览器兼容策略 | 已实现 | 已通过 |
| B-003 | 全局样式与加载态 | 根布局、全局样式和状态页 | 审查语义、可访问性、动效降级和全局副作用 | 已实现 | 已通过 |
| B-004 | 首页基础素材 | Logo、水墨背景、头像及 favicon 资源 | 确认继续使用、尺寸、格式、替代文本和加载策略 | 已实现 | 已通过 |
| B-005 | 迁移审查总表 | 旧项目产品文档与功能审查清单 | 建立可持续更新的迁移台账 | 已实现 | 已通过 |
| B-006 | 工程完成标准 | 旧项目 `check`、格式和文档校验脚本 | 明确新项目统一检查命令和测试策略 | 已实现 | 已通过 |
| B-007 | 环境与配置基线 | `.env.example`、`next.config.ts` | 只迁移确认需要的配置，避免携带密钥和过期兼容项 | 已实现 | 已通过 |

### 5.2 P1：首页静态体验

首页先迁移静态、可视部分，再接入最新文章数据，避免视觉重构和数据层重构混在一起。

| 编号 | 单元 | 旧项目参考 | 审查重点 | 实现状态 | 人工审查 |
| --- | --- | --- | --- | --- | --- |
| H-001 | 首页需求盘点 | `/`、`components/modules/home/` | 列出所有区块，决定保留、删除、合并或改版 | 已实现 | 已通过 |
| H-002 | 顶部品牌区与公共壳层 | 首页 Logo、标题和导航 | 语义层级、移动端布局、Logo 加载与替代文本 | 已实现 | 已通过 |
| H-003 | 首屏主视觉 | 水墨背景和 Hero 内容 | 首屏性能、对比度、暗色模式、响应式裁切 | 已实现 | 已通过 |
| H-004 | 首页静态区块 | 旧首页各展示区块 | 信息层级、重复组件、无效装饰和可访问性 | 已实现 | 已通过 |
| H-005 | 首页动态数据 | 最新文章 | 服务端获取、缓存、失败降级和空状态 | 已实现 | 已通过 |
| H-006 | 首页整体验收 | `/` | 桌面/移动端视觉、键盘操作、构建和性能 | 已实现 | 已通过 |

### 5.3 P2：公开站点与博客

| 编号 | 单元 | 旧项目参考 | 审查重点 | 实现状态 | 人工审查 |
| --- | --- | --- | --- | --- | --- |
| C-001 | 公开站点壳层 | `components/site/` | Header/Footer 职责、导航、响应式和复用边界 | 已实现 | 未审查 |
| C-002 | 元数据基础 | `lib/core/metadata.ts`、`site.ts` | 站点配置唯一来源、默认 OG 信息和 URL 生成 | 已实现 | 审查中 |
| D-001 | 数据库连接 | `lib/core/db.ts`、`sql/init.sql` | 连接池、环境校验、超时、错误边界和部署差异 | 已实现 | 已通过 |
| D-002 | 文章只读数据层 | `lib/domain/posts.ts`、`post-shared.ts` | SQL 正确性、分页、字段类型、空数据与错误处理 | 已实现 | 已通过 |
| BL-001 | 博客列表 | `/blog` | 分页、空状态、卡片语义、图片和 SEO | 已实现 | 已通过 |
| BL-002 | 文章详情 | `/blog/[slug]` | 动态参数、404、发布时间、结构化数据 | 已实现 | 未审查 |
| BL-003 | Markdown 渲染 | Markdown 相关组件与依赖 | XSS、链接策略、代码高亮、长内容排版 | 已实现 | 未审查 |
| BL-004 | 公开内容接口 | `/api/posts` | 分页参数、响应格式、缓存和限流 | 已实现 | 已通过 |
| BL-005 | RSS/Sitemap/Robots | 旧公开分发路由 | 内容完整性、规范 URL 和生产环境地址 | 未开始 | 未审查 |
| BL-006 | 错误与状态页 | `forbidden`、not-found、loading、error | 404/403/500 边界、用户反馈和可恢复性 | 未开始 | 未审查 |

### 5.4 P3：认证与后台壳层

认证相关代码必须按安全单元单独审查，不与后台业务页面混合迁移。

| 编号 | 单元 | 旧项目参考 | 审查重点 | 实现状态 | 人工审查 |
| --- | --- | --- | --- | --- | --- |
| A-001 | 用户数据模型 | 用户表、`types/` | 字段、角色、状态、唯一约束和数据迁移方式 | 未开始 | 未审查 |
| A-002 | 密码与 Session | `lib/core/auth.ts` | 哈希参数、Cookie 属性、签名、过期与撤销 | 未开始 | 未审查 |
| A-003 | 鉴权包装器 | `with-admin.ts`、`with-user.ts` | 权限边界、错误响应和重复查库策略 | 未开始 | 未审查 |
| A-004 | 登录/登出/当前用户 | `/api/auth/*` | 限流、枚举攻击、CSRF、输入校验和异常信息 | 未开始 | 未审查 |
| A-005 | 注册策略 | `/api/auth/register` | 是否保留公开注册、默认角色和滥用防护 | 未开始 | 未审查 |
| AD-001 | 后台登录页 | `/admin/login` | 表单语义、错误反馈、键盘操作和登录重定向 | 未开始 | 未审查 |
| AD-002 | 后台壳层 | `/admin/layout` | 服务端鉴权、导航结构、移动端和退出流程 | 未开始 | 未审查 |
| AD-003 | 后台概览 | `/admin` | 指标真实性、失败降级和无收益信息的删除 | 未开始 | 未审查 |

### 5.5 P4：内容管理

| 编号 | 单元 | 旧项目参考 | 审查重点 | 实现状态 | 人工审查 |
| --- | --- | --- | --- | --- | --- |
| CM-001 | 文章列表管理 | `/admin/posts` | 分页、筛选、空状态和批量操作边界 | 未开始 | 未审查 |
| CM-002 | 文章新增与编辑 | `/admin/posts/[id]` | 表单状态、草稿、并发覆盖、离开保护和预览 | 未开始 | 未审查 |
| CM-003 | 文章管理 API | `/api/admin/posts*` | 输入校验、权限、事务、错误响应和导出安全 | 未开始 | 未审查 |
| CM-004 | 分类管理 | categories 数据层和 API | 唯一约束、引用关系和删除规则 | 未开始 | 未审查 |
| CM-005 | 标签管理 | tags 数据层和 API | 唯一约束、引用关系和删除规则 | 未开始 | 未审查 |
| CM-006 | 图片上传 | upload/uploads 数据层和 API | MIME、大小、路径穿越、重复文件和清理策略 | 未开始 | 未审查 |
| CM-007 | 图片同步 | `/api/admin/uploads/sync` | 同步边界、幂等、失败恢复和生产目录权限 | 未开始 | 未审查 |
| CM-008 | 用户管理 | `/admin/users`、用户 API | 越权、禁用、删除、最后管理员保护和会话失效 | 未开始 | 未审查 |
| CM-009 | 设置与 SEO 推送 | `/admin/settings`、`seo-submit.ts` | 密钥处理、外部失败、URL 校验和操作反馈 | 未开始 | 未审查 |
| CM-010 | Showcase | `/admin/showcase*`、`src/showcase/` | 是否属于生产需求；若只用于开发，决定保留方式 | 未开始 | 未审查 |

### 5.6 P5：导航站

| 编号 | 单元 | 旧项目参考 | 审查重点 | 实现状态 | 人工审查 |
| --- | --- | --- | --- | --- | --- |
| N-001 | 导航站需求盘点 | `/nav`、`components/modules/nav/` | 区分核心功能与实验功能，确认保留范围 | 未开始 | 未审查 |
| N-002 | 搜索与书签 UI | 导航搜索和书签组件 | 键盘操作、URL 处理、分组排序和移动端体验 | 未开始 | 未审查 |
| N-003 | 游客本地存储 | `nav-storage.ts` | Schema 版本、损坏数据恢复、容量和隐私 | 未开始 | 未审查 |
| N-004 | 登录用户数据层 | `nav-db.ts`、`nav-*` 表 | JSON Schema、并发写入、数据大小和隔离 | 未开始 | 未审查 |
| N-005 | 游客/用户同步 | `/api/nav/data`、`sync` | 冲突规则、幂等、覆盖风险和失败恢复 | 未开始 | 未审查 |
| N-006 | 备忘录与笔记 | todos、notes API 和组件 | Markdown 安全、数据持久化和删除恢复 | 未开始 | 未审查 |
| N-007 | AI 模型与对话 | `/api/ai/*`、nav chat | 密钥、模型白名单、流式中断、费用和内容边界 | 未开始 | 未审查 |
| N-008 | Favicon 获取 | `/api/favicon` | SSRF、重定向、超时、响应大小和缓存 | 未开始 | 未审查 |

### 5.7 P6：网站统计“观澜”

统计模块写入量大且涉及隐私，必须先确认产品需求和数据保留策略，再迁移代码。

| 编号 | 单元 | 旧项目参考 | 审查重点 | 实现状态 | 人工审查 |
| --- | --- | --- | --- | --- | --- |
| T-001 | 统计需求与隐私 | 产品文档、统计表结构 | 采集字段、告知方式、IP 处理和保留期限 | 未开始 | 未审查 |
| T-002 | 站点管理 | `track-sites.ts`、站点 API | 所有权、域名校验、状态流转和密钥策略 | 未开始 | 未审查 |
| T-003 | 浏览器采集脚本 | `public/script.js` | 体积、兼容、重复加载、页面生命周期和隐私 | 未开始 | 未审查 |
| T-004 | 采集接口 | `/api/collect`、`track-events.ts` | 跨域、限流、伪造数据、批量写入和失败策略 | 未开始 | 未审查 |
| T-005 | 地理与 UA 解析 | `geo.ts`、`ua.ts` | 准确性、异常输入、更新机制和计算成本 | 未开始 | 未审查 |
| T-006 | 聚合与查询 | `analytics-*` | 指标口径、时区、去重、索引和大数据量性能 | 未开始 | 未审查 |
| T-007 | 统计后台页面 | `/admin/analytics*` | 图表正确性、日期口径、空状态和大列表性能 | 未开始 | 未审查 |
| T-008 | 清理与归档 | 事件和日聚合表 | 数据保留、删除权限、任务失败与可恢复性 | 未开始 | 未审查 |

### 5.8 P7：兼容、部署与发布

| 编号 | 单元 | 旧项目参考 | 审查重点 | 实现状态 | 人工审查 |
| --- | --- | --- | --- | --- | --- |
| R-001 | 旧 URL 重定向 | `legacy-redirects.ts` | 实际访问数据、循环重定向和永久/临时状态码 | 未开始 | 未审查 |
| R-002 | 环境变量 | `.env.example` | 必填/可选边界、启动校验和密钥泄漏防护 | 未开始 | 未审查 |
| R-003 | 数据迁移 | `sql/init.sql` 和生产数据 | 备份、Schema 版本、幂等、回滚和校验脚本 | 未开始 | 未审查 |
| R-004 | 部署配置 | `next.config.ts`、部署文档 | Node 版本、反向代理、上传目录和缓存策略 | 未开始 | 未审查 |
| R-005 | 全量回归 | 所有已通过条目 | 页面、API、权限、数据、SEO、可访问性和性能 | 未开始 | 未审查 |
| R-006 | 切换与回滚 | 线上环境 | 发布窗口、健康检查、日志、备份和回滚步骤 | 未开始 | 未审查 |

## 6. 单个迁移单元的执行模板

开始一个条目前，复制下面的模板到“迁移记录”。一个单元只维护一份记录。

```md
### [编号] 单元名称

- 实现状态：分析中
- 人工审查：未审查
- 旧项目入口：
- 新项目入口：
- 负责人：AI / 用户
- 对应提交：

#### 旧功能事实

- 用户入口：
- 正常流程：
- 异常和空状态：
- 依赖的数据/API/组件：

#### 旧代码审查发现

- [严重级别] 路径或位置：问题、影响、处理建议

#### 迁移决定

- 保留：
- 重写：
- 删除：
- 延后：

#### 验收标准

- [ ] 核心行为与确认后的需求一致
- [ ] 异常、空状态和权限边界明确
- [ ] 键盘操作和基本可访问性通过
- [ ] TypeScript、ESLint 和生产构建通过
- [ ] 已补充与风险相称的测试或人工验证证据
- [ ] 用户已审查并给出结论

#### 审查记录

- YYYY-MM-DD：待审查。
```

## 7. 每次人工审查应看到的内容

提交一个条目给用户审查时，说明必须控制在以下范围内：

1. **本次迁移了什么**：只描述本单元。
2. **旧实现发现了什么问题**：按严重程度列出，不粉饰旧代码。
3. **做了什么决定**：保留、重写、删除和延后的内容。
4. **如何验证**：命令结果、页面路径、截图或关键场景。
5. **需要用户判断什么**：视觉、交互、产品取舍或风险接受。
6. **明确当前状态**：在用户确认前保持 `已实现 / 未审查`。

## 8. 通用审查清单

并非每个单元都需要所有项目，但相关项不得跳过。

### 8.1 产品与行为

- 功能是否仍然有价值，是否存在更简单的实现？
- 正常、空数据、失败、加载和无权限状态是否完整？
- 是否保留了旧项目偶然形成、但没有产品价值的行为？
- 页面和 API 的边界是否清晰？

### 8.2 正确性与类型

- 是否存在边界条件、日期/时区、分页或并发问题？
- 不可信输入是否先校验，再进入业务逻辑？
- 是否滥用 `any`、类型断言或非空断言？
- 错误是否保留足够上下文，同时不向客户端泄漏敏感信息？

### 8.3 安全

- 鉴权是否发生在可信的服务端边界？
- 是否存在越权、XSS、CSRF、SSRF、SQL 注入或路径穿越风险？
- Cookie、Token、密钥和用户数据的处理是否安全？
- 上传、外部 URL 和开放写入接口是否有限制、超时和限流？

### 8.4 性能

- 是否出现重复请求、串行等待、无界查询或不必要的客户端组件？
- 图片、Markdown、图表和大列表是否有合理加载策略？
- 缓存是否会造成错误数据或权限数据串用？
- 数据库查询是否有明确分页和索引依据？

### 8.5 可访问性与视觉

- 页面结构、标题、表单标签和按钮语义是否正确？
- 键盘焦点、弹窗焦点管理、颜色对比度是否可用？
- 动画是否尊重 `prefers-reduced-motion`？
- 桌面、移动端、亮色和暗色模式是否都经过检查？

### 8.6 可维护性

- 依赖方向是否保持 `app -> domain -> core`，且职责清晰？
- 是否复制了本应删除的旧抽象或重复逻辑？
- 组件和函数是否足够小，但没有为了抽象而抽象？
- 文档、配置、Schema 和实际实现是否同步？

## 9. 决策记录

重要取舍在这里追加，禁止悄悄改变迁移方向。

| 日期 | 决策 | 原因 | 影响范围 | 状态 |
| --- | --- | --- | --- | --- |
| 2026-07-22 | 旧项目仅作为功能与行为参考，不直接视为可信代码 | 旧项目主要由 AI 生成且未逐项人工审查 | 全部迁移单元 | 已确认 |
| 2026-07-22 | 每个单元分开记录实现状态和人工审查状态 | 防止“代码已生成”被误认为“迁移已完成” | 全部迁移单元 | 已确认 |
| 2026-07-22 | 新项目升级到 Next.js 16，不保证旧 Next.js 15 实现可直接复用 | 框架版本和行为边界已经变化 | 路由、缓存、构建与部署 | 已确认 |
| 2026-07-22 | 使用 npm，并把 `package-lock.json` 作为依赖安装基线提交 | 确保不同环境安装相同依赖树 | 工程与部署 | 已确认 |
| 2026-07-22 | 开发和生产统一使用 Node.js 24 | 当前验证环境是 Node.js 24，且该版本处于长期支持周期 | 工程与部署 | 已确认 |
| 2026-07-22 | 目录随真实职责渐进创建，不预建空目录 | 避免在需求和复用关系出现前形成无收益结构 | 全部源码 | 已确认 |
| 2026-07-22 | 延续“文人书斋”的朱砂、宣纸和墨色体系 | 保留知简的品牌辨识度，避免通用 UI 配色 | 全站视觉 | 已确认 |
| 2026-07-22 | 暗色主题跟随系统，不在基础阶段增加手动切换 | 无脚本即可获得正确主题，避免提前增加状态与闪烁处理 | 全站主题 | 已确认 |
| 2026-07-22 | 全站采用零圆角的矩形语言 | 延续旧品牌最明确的形状特征，避免通用圆角组件观感 | 全站组件 | 已确认 |
| 2026-07-22 | 字体使用本地优先的系统栈，不加载远程字体 | 兼顾中文字体质量、首屏性能和隐私 | 全站排版 | 已确认 |

## 10. 迁移记录

### [B-001] 项目骨架审查

- 实现状态：已实现
- 人工审查：已通过
- 旧项目入口：根目录工程配置、旧项目 `package.json` 和 `tsconfig.json`
- 新项目入口：`package.json`、`package-lock.json`、`tsconfig.json`、`eslint.config.mjs`、`.gitignore`、`src/app/`
- 对应提交：`35c8aa6`、`3559805`、`cd66c20`

#### 旧功能事实

- 新项目已独立升级到 Next.js 16.2.11、React 19.2.4 和 TypeScript 5.9.3，不继承旧项目 Next.js 15 的兼容约束。
- 当前只包含 App Router 最小骨架，不存在业务组件、数据层或提前创建的空目录。
- 现有 ESLint 使用 Next.js Core Web Vitals 规则，App Router 根布局和占位首页可以正常构建；早期临时健康检查已在 B-003 后续整理中删除。

#### 旧代码审查发现

- [高] `.gitignore`：项目使用 npm，但 `package-lock.json` 被忽略且从未纳入 Git。新环境可能解析出不同的传递依赖，构建结果不可复现。已移除该忽略规则，并把现有锁文件纳入本次变更。
- [中] `package.json`：没有声明 Node.js 运行时版本，开发、CI 和生产可能使用不同的大版本。已声明 `>=24.0.0 <25`。
- [低] `tsconfig.json`：严格模式已开启，但没有显式检查未使用局部变量、未使用参数和文件名大小写一致性。已开启对应选项。
- [通过] `eslint.config.mjs`：当前配置足以覆盖最小骨架，未发现需要在 B-001 追加的规则。
- [通过] 目录边界：当前没有无收益抽象或空目录；后续按 `app -> domain -> core` 依赖方向渐进建立。
- [通过] 仓库卫生：构建产物、环境变量、本地日志和缓存均已忽略；未发现硬编码密钥或被 Git 跟踪的生成产物。

#### 迁移决定

- 保留 Next.js 16、React 19 和 TypeScript 5.9，不以兼容旧项目实现为目标。
- 使用 npm，提交 `package-lock.json`，后续 CI 和部署应使用锁文件安装。
- Node.js 24 作为开发和生产运行时基线。
- 保留 `strict`，并开启 `noUnusedLocals`、`noUnusedParameters`、`forceConsistentCasingInFileNames`。
- 保留当前最小 ESLint 配置；统一 `check`、格式化和测试策略放在 B-006 决定。
- 不预建 `components`、`lib`、`hooks` 等空目录，出现真实职责时再创建。
- B-001 初期曾保留 `/api/health` 作为部署健康检查；该临时接口已在 B-003 后续整理中删除，未来按实际部署方案重新决定。
- 当前 metadata 和首页均视为占位内容，分别在公开站点与首页单元中处理。

#### 验收标准

- [x] 框架、React、TypeScript、ESLint 和 Node.js 版本边界明确。
- [x] npm 锁文件与 `package.json` 内容一致，并已纳入版本控制。
- [x] TypeScript 严格规则通过现有代码。
- [x] ESLint 通过。
- [x] TypeScript `tsc --noEmit` 通过。
- [x] Next.js 生产构建通过。
- [x] `git diff --check` 通过。
- [x] 未发现硬编码密钥或被跟踪的构建产物。
- [x] 用户确认本节的版本、包管理器、目录边界和初期健康检查决定；后续删除记录见 B-003。

#### 审查记录

- 2026-07-22：完成工程骨架审查和最小修正。
- 2026-07-22：用户确认 B-001 通过。
- 2026-07-24：复核当前基线，早期临时 `/api/health` 已删除，后续维护记录见 B-003。

### [B-002] 设计令牌与主题审查

- 实现状态：已实现
- 人工审查：已通过
- 旧项目入口：`src/app/tokens.css`、`src/app/theme.css`、`docs/03-开发规范/04-样式规范.md`、`docs/03-开发规范/10-视觉风格指南.md`
- 新项目入口：`src/styles/tokens.css`、`src/styles/theme.css`
- 对应提交：`642c215`、`5e7624a`、`4068613`

#### 旧功能事实

- 旧项目的视觉方向是“文人书斋 · 水墨宣纸 · 温润雅致”，核心识别为朱砂、宣纸、墨色、衬线标题和零圆角。
- 新项目已经重建了更系统的基础色阶与语义变量，并保留随系统自动切换的暗色主题。
- 全局重置、焦点、选中态和动效降级属于 B-003，本单元只审查其所依赖的设计变量，不修改全局选择器。

#### 旧代码审查发现

- [中] `tokens.css`：新项目一次性定义了 137 个基础变量，项目全部 152 个变量中有 115 个没有任何引用，包含完整圆角、模糊、辅助色和多层 z-index 模板，与 B-001 的渐进建设原则冲突。已删除无明确消费者的模板变量，全部变量收口为 76 个，未使用基础刻度降至 39 个。
- [中] `theme.css`：亮暗主题的 `surface-raised` 都比 `surface-page` 更暗，变量名与视觉层级相反，未来卡片和浮层会出现错误层次。已调整为页面使用较深底色、抬升表面使用较亮底色。
- [低] `theme.css`：字体栈缺少完整的系统 UI、思源宋体、Linux 中文字体和现代 Windows 等宽字体回退。已补充跨平台本地字体栈，不引入远程字体请求。
- [通过] 色彩对比度：亮色正文 16.20:1、副文 5.25:1、主操作 7.08:1；暗色正文 17.87:1、副文 12.13:1、主操作 5.12:1，均达到 WCAG 2.2 AA 正文要求。
- [通过] 暗色模式：使用 `prefers-color-scheme`，并在对应主题中显式设置 `color-scheme`，不需要客户端状态。
- [通过] 兼容策略：保留 `color-mix()`，当前假设只支持仍在维护的现代浏览器；若未来需要旧版浏览器，再在部署基线中补充降级值。

#### 迁移决定

- 保留朱砂、宣纸、墨色的编辑式书斋视觉，主色不回退到旧项目更高饱和度的红色。
- 基础 Token 只保留有明确节奏的间距、排版、动效和当前主题实际依赖的色值；状态色和复杂层级在真实组件出现时再添加。
- 主题文件只暴露文本、表面、边框和主操作等语义角色，组件不直接依赖基础色阶。
- 亮色全局背景使用 `--color-paper-50`，抬升表面使用更浅的暖白 `--color-paper-0`；全局样式始终只引用 `--surface-*` 语义变量。
- 恢复零圆角作为品牌形状语言，不保留通用的 0 至 16px 圆角阶梯。
- 保留随系统自动切换的暗色主题，本阶段不增加手动切换器。
- 字体采用本地优先的跨平台栈：正文使用系统黑体，标题优先 Noto/思源宋体，代码使用系统等宽字体。
- 不在 B-002 修改 `globals.css` 或加载态；相关选择器、可访问性和动效在 B-003 单独审查。

#### 验收标准

- [x] 设计 Token 与主题语义职责清晰。
- [x] 删除无明确用途的圆角、模糊、辅助色和层级模板。
- [x] 所有 `var(--*)` 引用均有定义。
- [x] 关键亮暗主题组合达到 WCAG 2.2 AA 对比度。
- [x] ESLint 通过。
- [x] TypeScript `tsc --noEmit` 顺序执行通过。
- [x] Next.js 生产构建通过。
- [x] `git diff --check` 通过。
- [x] 用户确认色彩方向、自动暗色、零圆角和本地字体策略。

#### 审查记录

- 2026-07-22：完成设计令牌与主题审查和最小收口，等待用户审查。
- 2026-07-22：根据用户反馈将亮色全局背景调整为 `--color-paper-50`，并保留更亮的抬升表面层级。
- 2026-07-22：完成最终审查，用户确认 B-002 通过。

### [B-003] 全局样式与基础状态审查

- 实现状态：已实现
- 人工审查：已通过
- 旧项目入口：`src/app/layout.tsx`、`src/app/globals.css`、`src/app/loading.tsx`、`src/app/loading.module.css`、`src/types/app.ts`
- 新项目入口：`src/app/layout.tsx`、`src/styles/globals.css`、`src/app/loading.tsx`、`src/app/loading.module.css`、`src/types/app.ts`
- 对应提交：`5e7624a`、`8943502`、`09ec5a6`、`f9c12f3`

#### 旧功能事实

- 旧项目在根布局加载全局样式，由 `globals.css` 同时承担样式重置、滚动条、背景蒙版、页面容器和无障碍工具类。
- 旧项目的全局加载态是 Next.js App Router 的 Suspense fallback，使用全屏背景、衬线文字和三个渐显圆点。
- 新项目继续保留全局加载态与共享根布局类型；临时 `/api/health` 接口已在后续工程整理中删除。

#### 旧代码审查发现

- [中] `globals.css`：旧实现把页面容器、背景蒙版、滚动条和未确认消费者的工具类放入全局作用域，容易形成跨页面耦合。新实现只保留页面基础样式、表单字体、媒体约束、选中态、焦点和动效降级，页面样式后续使用 CSS Modules。
- [中] `loading.tsx`：旧实现没有状态语义，辅助技术无法可靠感知路由正在加载。新实现使用 `role="status"` 和可见的“加载中”文本，并将装饰圆点从播报内容中排除。
- [低] `loading.tsx`：旧实现使用 `<i>` 承载无语义装饰字符，还需要额外重置字体样式。新实现改用 `<span>`，结构与用途一致。
- [通过] `globals.css` 与 `loading.module.css`：平滑滚动和圆点动画均响应 `prefers-reduced-motion`，焦点轮廓保持可见。

#### 迁移决定

- 使用 `modern-css-reset` 负责通用重置，项目全局样式只维护当前真实需要的基础规则。
- 全站设计样式集中在 `src/styles`；组件私有的 `loading.module.css` 继续与组件就近放置。
- 根布局保持 `reset -> tokens -> theme -> globals` 的加载顺序。
- 不迁移旧项目的全局滚动条、页面容器、背景蒙版和无消费者工具类；出现实际需求时再就近实现。
- 保留具备状态语义的全局加载态和已确认的动画节奏，减少动态效果时停用动画。
- 保留 `src/types/app.ts` 统一放置项目类型，不为当前单一类型继续拆分目录。
- 已删除临时 `/api/health`，未来部署健康检查与正式 API 按实际部署方案单独审查。

#### 验收标准

- [x] 全局样式职责明确，没有迁移无消费者的旧全局类。
- [x] 加载态具有可感知的状态文本，装饰内容不会重复播报。
- [x] 焦点样式可见，非必要动画支持 `prefers-reduced-motion`。
- [x] 全局设计样式与组件私有样式的目录边界明确。
- [x] ESLint 通过。
- [x] TypeScript `tsc --noEmit` 通过。
- [x] Next.js 生产构建通过。
- [x] `git diff --check` 通过。
- [x] 用户已人工查看并确认加载态视觉与动画节奏。
- [x] 用户确认 B-003 的全局样式、加载态和共享类型处理方式。
- [x] 已删除临时 `/api/health`，不保留未维护的 API 路由。

#### 审查记录

- 2026-07-23：完成代码审查，未发现阻断问题；记录临时健康检查的缓存风险。
- 2026-07-23：根据用户决定暂不修改健康检查，后续直接删除；提交最终人工确认。
- 2026-07-23：用户确认 B-003 通过。
- 2026-07-24：临时 `/api/health` 已由 `f9c12f3` 删除；B-003 当前基线不再包含 API 路由。

### [B-004] 首页基础素材审查

- 实现状态：已实现
- 人工审查：已通过
- 旧项目入口：`public/images/logo.webp`、`public/images/bg-landscape.webp`、`public/favicon.ico`、favicon PNG 与 Apple Touch Icon 及其使用位置
- 新项目入口：`public/images/logo.webp`、`public/images/bg-landscape.webp`、`public/images/profile.webp`、`public/images/og-default.webp`、`public/favicon.ico`、`public/images/favicon-16x16.png`、`public/images/favicon-32x32.png`、`public/images/apple-touch-icon.png`
- 对应提交：`d0e216e`、`6d72e5c`

#### 旧功能事实

- `logo.webp` 是带英文 “ZHIJIAN” 字标的完整方形品牌图，尺寸为 1024 × 1024，文件大小为 23,628 字节，采用不透明 RGB WebP。
- `bg-landscape.webp` 是首页首屏使用的水墨山水横图，尺寸为 1376 × 768，文件大小为 26,586 字节，采用不透明 RGB WebP。
- 两张新项目资源与旧项目对应文件的 SHA-256 完全一致，确认是未经重新编码的原样迁移。
- 旧项目还把 Logo 用于 32px 导航图标、个人头像、后台和状态页，并把山水图作为首页全屏背景及状态页背景。
- 用户为新项目补充了独立个人头像；原始 JPG 为 640 × 640、49,227 字节，不含 EXIF、GPS、方向或色彩配置元数据。
- 旧项目 favicon 资源组包含 32 × 32 ICO、16 × 16 与 32 × 32 PNG，以及 180 × 180 Apple Touch Icon，并由根布局元数据引用。

#### 旧代码审查发现

- [中] `logo.webp`：图片包含大面积白色不透明背景和完整英文文字，直接缩小到 32px 或放在暗色、透明背景上会出现白色方块，字标也难以辨认。后续不能照搬旧项目的小尺寸导航和暗色场景用法。
- [中] 旧图片替代文本：导航 Logo 与相邻品牌文字重复播报，首页个人卡片把品牌 Logo 描述成作者头像，装饰性山水背景则使用了有意义的替代文本。新实现需要按实际语义区分品牌内容和纯装饰图片。
- [低] `bg-landscape.webp`：1376px 宽度用于高分辨率全屏背景时会被放大，移动端 `cover` 裁切还可能丢失左侧小舟。水墨风格可以容忍一定柔化，但需要在 Hero 实现时检查宽屏清晰度和移动端构图。
- [通过] 文件格式与体积：两张图均为 WebP，单文件不足 27KB，当前没有必要为了格式或体积重新压缩，也不需要提前增加备用格式。
- [通过] 当前引用：新项目尚未在页面中使用这三张资源，不存在现阶段的布局偏移、错误替代文本或首屏加载回归。
- [通过] `profile.webp`：头像保持 640 × 640 原尺寸，以 WebP 质量 85 编码后为 34,206 字节，约比原 JPG 减少 30%，视觉检查未发现可感知劣化，输出不包含元数据。
- [低/已接受] 旧 favicon：图标包含完整图形和英文小字，在 16px、32px 标签页中辨识度有限。用户决定现阶段继续使用原资源组，后续如有需要可单独替换 favicon，不影响主 Logo。

#### 迁移决定

- 保留两张原始 WebP；用户确认 `public/images/logo.webp` 继续作为正式主 Logo，保持文件和视觉不变，不重绘、不替换。
- `logo.webp` 仅用于尺寸足够且具有浅色承载面的完整品牌展示；不直接用于小尺寸导航图标、头像、favicon 或暗色背景。
- H-002 继续使用现有主 Logo；若站点需要 favicon 等小尺寸图标，后续作为独立资源处理，不改变或替换主 Logo。
- 按用户决定原样迁移旧 favicon 资源组，并在根布局 `metadata.icons` 中声明浏览器图标与 Apple Touch Icon；不迁移旧 `manifest.json`，避免提前引入未审查的 PWA 配置。
- `bg-landscape.webp` 作为装饰性背景使用时采用空替代文本；在 H-003 中根据实际 Hero 布局确定 `object-position`、主题遮罩和移动端裁切。
- 首页主视觉若成为 LCP 候选，使用 Next.js `Image` 的响应式尺寸和 `preload`；其他非首屏使用不抢占关键加载资源。
- 使用 `profile.webp` 作为个人头像，不再复用品牌 Logo；与可见姓名相邻时使用空替代文本，单独表达身份时使用作者名称作为替代文本。

#### 验收标准

- [x] 已核对文件格式、尺寸、体积和透明通道。
- [x] 已确认新旧文件哈希一致，没有迁移损坏或隐式重编码。
- [x] 已完成两张资源的原始尺寸视觉检查。
- [x] 已明确品牌图片与装饰背景的替代文本策略。
- [x] 已明确首屏加载、响应式裁切和暗色主题风险。
- [x] 用户确认继续保留 Logo、水墨背景及其使用边界。
- [x] 独立头像已转换为经过视觉验证且不含元数据的 WebP。
- [x] favicon 资源组已原样迁移并接入 Next.js 元数据。
- [x] favicon 新旧文件哈希一致，旧 manifest 未混入本单元。
- [x] 用户确认转换后的 `profile.webp`。

#### 审查记录

- 2026-07-23：完成素材元数据、文件哈希、视觉质量、旧用途、可访问性和加载策略审查，等待用户确认。
- 2026-07-23：用户确认 Logo 和水墨背景的使用边界，并补充独立个人头像。
- 2026-07-23：将 `profile.jpg` 转换为 `profile.webp` 并移除原 JPG，等待最终人工确认。
- 2026-07-23：用户确认继续使用现有 `logo.webp`，主 Logo 不重绘、不替换。
- 2026-07-23：根据用户决定迁移旧 favicon 资源组并接入根布局，记录小尺寸辨识度限制；旧 manifest 暂不迁移。
- 2026-07-23：用户完成本单元最终审查，确认头像及 favicon 资源配置，B-004 标记为已通过。

### [B-005] 建立迁移审查总表

- 实现状态：已实现
- 人工审查：已通过
- 旧项目入口：`README.md`、`AGENTS.md`、`docs/01-产品文档/02-功能文档.md`、`docs/03-开发规范/11-功能审查清单.md`
- 新项目入口：`docs/00-迁移审查总表.md`
- 对应提交：`cd66c20`、`c6c0287`

#### 旧功能事实

- 旧项目已有较完整的功能说明和代码清单，但它们描述的是旧项目现状，不是迁移进度。
- 新项目在本文档创建前没有迁移清单，也没有区分实现完成与人工审查通过。

#### 文档审查发现

- [中/已修复] 项目路径最初使用机器相关的绝对路径，在不同电脑上会产生文档漂移。已改为以新项目仓库根目录为基准的 `.` 与 `../zhijian`。
- [中/已修复] P0 完成标准只覆盖 B-001 至 B-005，但详细清单还包含 B-006、B-007，底部下一步也遗漏 B-007。已统一为 B-001 至 B-007，并补全执行顺序。
- [低/已修复] B-002、B-004 和 B-005 的提交引用仍包含“本次提交”或“待提交”等临时文本。已替换为仓库中的真实提交哈希。
- [低/已修复] 文档缺少状态、适用范围、核对日期和维护范围，难以判断内容是否仍是当前基线。已补充最小文档元信息。
- [通过] 新旧项目规模统计已按实际文件重新核对，页面、API、组件、样式、核心目录及静态资源数量与当前记录一致。
- [通过] 当前 `docs` 只有本表一份正式文档，继续保持单文件入口更直接；出现职责和更新频率不同的正式文档后再建立导航，不提前拆分空结构。

#### 迁移决定

- 保留旧文档作为功能发现入口，不直接复制到新项目。
- 新建一份以迁移单元为核心的总表。
- 文档建立时将历史基础提交先记为“已实现 / 未审查”，之后按迁移单元逐项完成人工审查。
- 后续只在对应单元完成时更新状态，不能整阶段提前标记完成。
- 本表继续作为迁移状态的唯一来源；后续产品或技术文档只描述各自职责，不重复维护迁移进度。
- 当前不为单份文档新建导航文件；正式文档增多后再补充仓库文档入口。

#### 验收标准

- [x] 记录并复核新旧项目路径和当前基线。
- [x] 覆盖旧项目主要页面、API 和业务模块。
- [x] 定义实现状态与人工审查状态。
- [x] 提供逐单元执行和审查模板。
- [x] 路线阶段、详细清单和下一步顺序保持一致。
- [x] 已使用真实提交哈希替换临时引用。
- [x] 用户确认迁移拆分粒度和顺序。
- [x] 用户确认本文档可作为后续唯一进度基线。

#### 审查记录

- 2026-07-22：文档已生成，等待用户审查。
- 2026-07-23：完成路径、规模、状态、提交引用和路线顺序复核，修正文档漂移，等待用户最终确认。
- 2026-07-23：用户确认迁移拆分粒度、执行顺序和唯一进度基线职责；项目路径改用跨机器可复用的相对路径，B-005 标记为已通过。

### [B-006] 工程完成标准

- 实现状态：已实现
- 人工审查：已通过
- 旧项目入口：`package.json`、`scripts/docs-check.mjs`、`.prettierrc.json`、`.prettierignore`
- 新项目入口：`package.json`
- 对应提交：`0ab07aa`

#### 旧功能事实

- 旧项目的 `check` 依次运行 ESLint、TypeScript 和文档检查，不包含生产构建。
- 旧项目使用 Prettier 提供格式化命令，其文档脚本强依赖完整的产品、技术、接口和开发规范目录。
- 新项目在本单元之前已有独立的 `lint`、`typecheck` 和 `build`，但没有统一完成入口或测试策略。

#### 旧代码审查发现

- [中] 新项目缺少统一检查入口，每次迁移需要手动记住并分别运行三条命令，容易漏掉类型检查或生产构建。
- [中/不迁移] 旧 `docs-check.mjs` 硬编码了新项目尚不存在的文档和 API 目录，直接复制会制造空文档结构或让检查永久失败；用户决定后续文档独立管理，不接入项目自动门禁。
- [低] 旧项目的 `check` 不包含生产构建，无法覆盖 Next.js 路由约束、服务端边界和构建期错误。
- [低/已接受] 当前还没有业务逻辑、数据转换或复杂交互，为空测试集引入测试框架不能提供有效质量信号。
- [低/已接受] 当前文件数量少且已有稳定格式，不为建立形式上的格式化命令立即引入 Prettier 和全仓格式化变更。

#### 迁移决定

- 新增 `npm run check`，顺序执行 ESLint、TypeScript 和 Next.js 生产构建，作为每个迁移单元完成前的统一代码门禁。
- 不迁移 `docs:check`，文档由用户独立管理，不与代码检查命令绑定。
- 暂不引入 Prettier；出现多人协作、明显格式漂移或需要统一格式时，作为独立变更确定配置并审查格式化结果。
- 暂不新增空的 `test` 命令或测试框架；首个可独立验证的业务逻辑、权限边界或数据转换应在对应迁移单元中补测试，并将测试命令接入 `check`。
- UI 单元根据风险使用浏览器人工验证或自动化测试，不以低价值快照测试替代真实交互检查。
- `git diff --check` 继续作为提交前检查执行，不写入 `check`，避免在干净的 CI 工作区中形成无效门禁。

#### 验收标准

- [x] `npm run check` 覆盖 lint、类型检查和生产构建。
- [x] 文档检查脚本已移除，文档维护不与代码门禁耦合。
- [x] 未为没有消费者的格式化和测试能力提前增加依赖。
- [x] 明确测试框架和格式化工具的后续引入条件。
- [x] 用户确认统一检查范围和执行顺序。
- [x] 用户确认当前测试与格式化策略。

#### 审查记录

- 2026-07-23：完成旧工程脚本审查，建立当前项目最小质量门禁，等待用户审查。
- 2026-07-23：根据用户反馈移除 `docs:check`，文档后续由用户独立管理，统一代码门禁收敛为 lint、类型检查和生产构建。
- 2026-07-23：用户确认统一代码门禁及当前测试、格式化策略，B-006 标记为已通过。

### [B-007] 环境与配置基线

- 实现状态：已实现
- 人工审查：已通过
- 旧项目入口：`.env.example`、`.gitignore`、`next.config.ts` 及环境变量消费者
- 新项目入口：`.gitignore`、`package.json`、`tsconfig.json`
- 对应提交：`8c5e646`

#### 旧功能事实

- 旧项目的环境变量服务于站点 URL、统计脚本、Google Analytics、数据库、管理员初始化、Session、SEO 推送、DeepSeek 和 GitHub 数据。
- 旧 `next.config.ts` 配置了 `ip2region` 外部包、旧 URL 永久重定向和 HTML 缓存响应头，并包含空的 `turbopack` 配置。
- 新项目当前没有读取任何环境变量，没有 `.env` 文件被 Git 跟踪，也没有 `next.config.*`；Next.js 默认配置下可以完成生产构建。

#### 旧代码审查发现

- [中/已修复] `next-env.d.ts` 按 Next.js 16 要求被 Git 忽略，但原 `typecheck` 直接运行 `tsc`，全新环境可能尚未生成 Next.js 声明和路由类型。已在 TypeScript 检查前增加 `next typegen`。
- [中/不迁移] 旧 `.env.example` 中的变量全部属于尚未迁移的业务能力，提前复制会把不存在的功能误写成当前部署要求。
- [中/不迁移] 旧 `next.config.ts` 的外部包、重定向和缓存规则均没有当前消费者；其中全站 HTML 缓存头还会改变 Next.js 默认缓存语义，不能作为基础配置直接迁移。
- [通过] 当前 `.gitignore` 忽略 `.env*` 并仅允许 `.env.example`，能够防止真实环境文件进入版本控制，同时为未来模板保留入口。
- [通过] 当前源码、工程配置和已跟踪文件中未发现硬编码凭据或业务环境变量依赖。

#### 迁移决定

- 保留现有环境文件忽略规则，不创建没有消费者的 `.env.example`。
- 首个真实环境变量随所属迁移单元引入，并在同一单元补充不含真实凭据的 `.env.example`、必填/可选说明和服务端边界。
- 暂不创建空的 `next.config.ts`；外部包、重定向、响应头或其他 Next.js 配置在出现明确需求时分别审查。
- `typecheck` 先运行 `next typegen`，确保被忽略的 `next-env.d.ts` 和 `.next/types` 在独立类型检查前生成。
- 旧环境变量按业务归属延后到对应单元，不集中提前迁移；`NODE_ENV` 由 Next.js 管理，不进入环境变量模板。

#### 验收标准

- [x] 已核对旧项目环境变量及其真实消费者。
- [x] 当前项目没有未声明的环境变量依赖或被跟踪的环境文件。
- [x] `.gitignore` 能阻止真实环境文件提交并允许未来的示例模板。
- [x] 未迁移没有消费者的旧 Next.js 配置。
- [x] 独立类型检查会先生成 Next.js 类型声明。
- [x] `npm run check` 在当前默认配置下通过。
- [x] 用户确认环境变量按功能延后迁移。
- [x] 用户确认当前不创建 `.env.example` 和 `next.config.ts`。

#### 审查记录

- 2026-07-23：完成环境变量、Git 忽略规则、Next.js 配置和类型生成流程审查。
- 2026-07-23：用户确认环境变量按功能延后迁移，当前不创建 `.env.example` 和 `next.config.ts`，B-007 标记为已通过，P0 标记为已完成。

### [H-001] 首页需求盘点

- 实现状态：已实现
- 人工审查：已通过
- 旧项目入口：`src/app/page.tsx`、`src/components/modules/home/`
- 新项目入口：`src/app/(public)/page.tsx`（已接入首屏、个人信息、最新文章和开源项目；GitHub 提交热力图已确认不迁移）
- 对应提交：`5e43e59`、`4930390`

#### 旧首页结构事实

旧首页由服务端页面编排四个内容区块，页面本身不负责公共导航和页脚：

| 区块 | 旧实现 | 内容与依赖 |
| --- | --- | --- |
| 首屏主视觉 | `HeroSection` | 水墨山水背景、作者名、签名行、首屏导语和跳转到 `#about-me` 的 CTA；无业务数据依赖 |
| 个人信息 | `ProfileSection` | `ProfileCard` 展示头像、简介、邮箱、GitHub、RSS；`CommitChart` 展示最近约 26 周提交热力图 |
| 最新文章 | `PostsSection` | 服务端读取最近 3 篇已发布文章，展示文章卡片和空状态，并链接到 `/blog` |
| 开源项目 | `ProjectsSection` | 页面内静态配置 `zhijian` 与 `cesium-example` 两个项目卡片，包含技术标签和外链操作 |

页面级行为：

- `page.tsx` 以 `Promise.all` 并行读取文章与 GitHub 提交记录，并设置 `dynamic = 'force-dynamic'`。
- 页面生成 `WebSite`、`Person`、`CollectionPage` 和最新文章 `ItemList` JSON-LD。
- 数据层未配置数据库时返回空文章列表；未配置 `GITHUB_TOKEN` 时提交热力图显示空状态，不阻塞页面渲染。
- 公共导航、页脚、统计脚本和透明导航状态由旧项目的 `PublicChrome` / `AppFrame` 提供，属于后续站点壳层单元，不在 H-001 迁移。

#### 迁移范围边界

- H-001 只确认首页内容区块和信息层级，不复制旧组件、样式或数据层。
- Logo、山水背景、头像和 favicon 资源已在 B-004 处理；本单元只确认它们在首页中的语义用途。
- 博客查询、公共导航、元数据和 JSON-LD 的具体实现分别在 H-005、C-001、C-002 及博客单元中审查；旧 GitHub API 不进入新项目。

#### 迁移审查发现

- 当前新首页已接入首屏、个人信息、最新文章和开源项目区；最新文章已有服务端数据消费者，GitHub 热力图已从迁移范围删除。
- 首屏背景属于关键视觉资源，迁移时需要重新按当前 tokens、主题和移动端裁切规则实现，不能直接复制旧 CSS。
- 旧个人卡片把品牌 Logo 作为头像使用；新项目改用独立头像资源，头像语义和替代文本在 H-004 实现时确认。
- GitHub 热力图属于可选动态增强，用户已确认不迁移，后续不引入对应组件、服务端查询、Token 或缓存配置。
- 首页已有 `src/config/home.ts` 作为静态文案入口；项目数据应随同首屏与个人信息配置集中维护，不为当前两个项目预建通用卡片数据层。

#### 待用户确认

| 决策项 | 旧首页内容 | 可选方向 | 用户决定 |
| --- | --- | --- | --- |
| A | 首屏主视觉 | 保留并重写 / 删除 / 改为紧凑首屏 | 保留并按新主题重写 |
| B | 个人信息卡片 | 保留 / 精简 / 删除 | 保留，使用独立头像资源 |
| C | GitHub 提交热力图 | 保留 / 从个人区拆出 / 删除 | 删除，不迁移 |
| D | 最新文章 | 保留 3 篇 / 调整数量 / 暂缓到博客完成 | 保留最近 3 篇，数据接入延后到博客只读链路 |
| E | 开源项目 | 保留并更新 / 只保留一个 / 删除 | 保留并更新项目与链接 |

#### 验收标准

- [x] 已核对旧首页入口和全部首页内容组件。
- [x] 已记录四个内容区块、数据依赖和公共壳层边界。
- [x] 已区分静态内容、可选动态内容和后续迁移单元。
- [x] 用户确认保留 A、B、D、E，删除 C 的 GitHub 提交热力图。

#### 审查记录

- 2026-07-23：完成旧首页入口、内容区块、数据依赖和迁移边界盘点，等待用户审查。
- 2026-07-23：用户确认首屏、个人信息、GitHub 提交热力图、最新文章和开源项目全部保留，H-001 标记为已通过。
- 2026-07-26：用户调整首页范围，明确取消 GitHub 提交热力图；GitHub 个人主页和项目仓库外链继续保留。

### [H-002] 顶部品牌区与公共壳层

- 实现状态：已实现
- 人工审查：已通过
- 旧项目入口：`src/app/layout.tsx`、`src/components/site/app-frame.tsx`、`src/components/site/public-chrome.tsx`、`src/components/site/site-header.tsx`
- 新项目入口：`src/app/layout.tsx`、`src/app/(public)/layout.tsx`、`src/app/(public)/layout.module.css`、`src/app/(public)/page.tsx`、`src/app/(public)/blog/page.tsx`、`src/components/site/site-header.tsx`、`src/components/site/site-footer.tsx`、`src/components/ui/icon-button.tsx`、`src/hooks/use-scroll-threshold.ts`
- 对应提交：`449390a`、`015309a`、`4035de4`

#### 旧公共壳层事实

| 能力 | 旧实现 | 当前边界 |
| --- | --- | --- |
| 路由分发 | `AppFrame` 根据 pathname 将 `/admin/*` 和 `/nav` 排除在公共壳层外，其余页面包裹 `PublicChrome` | 新项目尚无这些路由，暂不迁移分发逻辑 |
| 顶部导航 | `SiteHeader` 提供 Logo、桌面导航、移动端菜单和当前路由高亮 | 已迁移首页与 `/blog`；`/blog` 当前仅为占位列表路由，用于验证导航状态 |
| 首页透明状态 | 首页和博客路径使用 fixed 透明导航，滚动超过 10px 后增加背景和毛玻璃 | 已使用阈值 `8px` 的滚动状态与主题模糊令牌实现 |
| 普通页面状态 | 其他公开页面使用 sticky 导航和毛玻璃背景 | 由公共站点壳层统一承载 |
| 页脚 | `PublicChrome` 提供版权文案和站点背景 | 已在公开布局中迁移版权文案；统计脚本继续后置 |
| 统计脚本 | 根据统计环境变量通过 `next/script` 注入自建统计和 GA | 随统计单元和环境变量消费者迁移，不在 H-002 引入 |

#### 旧导航交互事实

- 品牌链接使用 `logo.webp`，桌面端在 640px 以上显示品牌文字，窄屏只显示图标。
- 桌面端从 `PUBLIC_NAV_ITEMS` 生成导航；当前旧配置包含首页和文章两个入口。
- 移动端使用原生 `button` 打开菜单，设置 `aria-expanded`、`aria-controls` 和可访问名称；路由切换、点击外部和关闭操作会关闭菜单并归还触发按钮焦点。
- 透明模式通过 `scroll` 事件更新状态，监听器在组件卸载或模式改变时清理。

#### 迁移审查发现

- 当前公开路由布局已接入 `SiteHeader` 和 `SiteFooter`；壳层使用 flex 布局，使短页面的页脚保持在视口底部。
- 旧导航依赖 `PUBLIC_NAV_ITEMS`、`SITE_METADATA`、图标和 `useClickOutside` 等多个旧模块，不能以单文件复制方式迁移。
- 旧 CSS 使用旧主题变量，必须按新项目 tokens/theme 重写；不直接复制 `site-header.module.css` 或 `public-chrome.module.css`。
- 旧移动菜单使用 `role="dialog"`，但当前只有两个公开入口，改为右侧并排的图标链接，避免引入无收益的菜单焦点管理。
- 统计脚本属于未迁移的业务能力，保留壳层插槽即可，不提前添加环境变量或第三方脚本。

#### 待用户确认

| 决策项 | 可选方向 | 用户决定 |
| --- | --- | --- |
| A | 保留 Logo + 品牌文字 / 只保留 Logo / 重做品牌区 | 保留 Logo + 品牌文字，使用当前 `public/images/logo.webp` |
| B | 保留首页透明导航 / 首页也使用普通导航 / 不做透明状态 | 保留首页透明导航 |
| C | 导航先只放首页 / 同时预留文章入口 / 暂不显示导航 | 显示已存在的首页与 `/blog` 占位路由 |
| D | 保留移动端折叠菜单 / 移动端直接显示导航 / 暂缓移动端 | 直接显示右侧图标导航，不使用弹出菜单 |
| E | 当前单独迁移页脚 / 与公共壳层一起迁移 / 暂缓 | 与公共壳层一起迁移，统计脚本后置 |

#### 验收标准

- [x] 已核对旧根布局、路由分发、公共壳层和顶部导航入口。
- [x] 已记录桌面、移动、透明滚动和页脚职责边界。
- [x] 已识别 `/blog` 仅为占位路由、旧主题变量和统计环境变量等迁移风险。
- [x] 用户确认 A-E 五项品牌区与公共壳层取舍。
- [x] 已建立公开路由分组，首页 URL 保持为 `/`。
- [x] 用户确认保留 `(public)` 路由分组，作为公开站点的长期布局边界。
- [x] 已接入桌面品牌区、Logo、品牌文字、首页与博客导航入口和当前路由状态。
- [x] 用户确认 H-002A/B 页面效果，当前代码审查未发现阻塞项。
- [x] 已接入移动端图标导航与滚动阈值状态；滚动后使用主题背景和模糊效果。
- [x] 已迁移公共页脚，保留版权文案，未提前接入统计脚本。

#### 审查记录

- 2026-07-23：完成旧公共壳层、顶部导航、移动菜单和依赖边界盘点，等待用户审查。
- 2026-07-23：用户确认保留 Logo、首页透明导航、移动端折叠菜单和页脚；导航暂只显示首页，统计脚本后置。需求边界已确认，代码实现与人工审查尚未开始。
- 2026-07-23：完成 H-002A 公开路由边界迁移：首页移入 `(public)` 路由分组，URL 保持为 `/`；导航、页脚和客户端交互留待后续子步骤。
- 2026-07-23：用户确认保留 `(public)` 路由分组结构，后续公开页面统一纳入该边界，后台和导航站按实际布局需求再建立独立分组。
- 2026-07-23：完成 H-002B 品牌区初步迁移：接入 `logo.webp`、`Zhijian` 品牌文字和首页入口，使用透明固定顶部布局；移动端菜单、滚动状态和页脚留待后续子步骤。
- 2026-07-23：用户确认 H-002A/B 当前页面效果无问题；代码审查未发现阻塞项，共享布局类型由 `RootLayoutProps` 收口为语义更准确的 `AppLayoutProps`。H-002 保持“实现中 / 审查中”，等待后续子步骤完成。
- 2026-07-24：已接入首页与 `/blog` 导航状态、移动端图标导航和滚动阈值背景；不迁移旧弹出菜单，公共页脚仍后置。
- 2026-07-25：完成公共页脚代码迁移：保留旧站版权文案，使用当前主题 token 和公开壳层 flex 布局；未迁移统计脚本。ESLint、TypeScript、生产构建与首页、`/blog` 本地响应验证通过，等待用户视觉审核。
- 2026-07-27：用户确认 H-002 通过。

### [H-003] 首屏主视觉

- 实现状态：已实现
- 人工审查：已通过
- 旧项目入口：`src/components/modules/home/hero-section.tsx`、`hero-section.module.css`
- 新项目入口：`src/app/(public)/page.tsx`、`src/features/home/ui/hero-section.tsx`、`src/features/home/ui/hero-section.module.css`
- 对应提交：`5e43e59`

#### 旧首屏事实

- `HeroSection` 使用 `next/image` 的 `fill`、`priority` 和 `sizes="100vw"` 加载 `/images/bg-landscape.webp`，背景覆盖整个首屏并使用 `object-fit: cover`。
- 首屏内容包含作者名、签名行、导语和“开始探索” CTA；CTA 锚定首页个人信息区 `#about-me`。
- 背景上方有覆盖层，内容位于覆盖层之上；旧 CSS 使用旧主题变量控制文字、边框和间距。
- 首屏最小高度为 `100vh`，桌面和移动端共用垂直居中布局，内容最大宽度约 52rem。
- 首屏没有业务数据请求，文案来自旧站点配置；主视觉资源已在 B-004 迁移。

#### 迁移审查发现

- 新项目已经有 `bg-landscape.webp`，但旧首屏样式依赖旧变量，不能直接复制 CSS；需要改用当前 tokens/theme 和 CSS Module。
- `priority` 适合首屏高概率使用的背景，但最终仍需在真实页面确认 LCP、移动端图片裁切和首屏下方内容是否可见。
- 旧首屏使用全屏高度，可能在窄屏设备上挤压后续内容；实现时需要保留首屏视觉完整性，同时保证用户能发现并操作 CTA。
- 覆盖层、文字和 CTA 的对比度必须在明暗主题、背景不同裁切位置下重新验证，不能仅凭旧颜色判断。
- `prefers-reduced-motion` 需要覆盖首屏滚动行为或后续入场动效；静态首屏本身不应依赖动画传达信息。

#### 待用户确认

| 决策项 | 可选方向 | 用户决定 |
| --- | --- | --- |
| A | 保留全屏首屏 / 缩短为首屏带 / 删除背景图 | 桌面和移动端均保留全屏首屏 |
| B | 保留原文案 / 重写文案 / 只保留作者名 | 保留现有文案层级，后续重写具体文案 |
| C | 保留“开始探索”锚点 CTA / 改为博客入口 / 暂无 CTA | 保留锚点 CTA，目标为个人信息区 |
| D | 保留水墨图覆盖层 / 重新设计覆盖层 / 不使用覆盖层 | 保留水墨背景，按新主题重新设计覆盖层 |

#### 验收标准

- [x] 已核对旧首屏组件、背景资源、文案层级和 CTA 目标决策。
- [x] 已记录首屏性能、响应式裁切、对比度和动效降级风险。
- [x] 用户确认 A-D 四项首屏方向。
- [x] 已使用 Server Component、Next.js Image 和 CSS Module 实现全屏首屏。
- [x] 桌面和移动端均使用 `100svh` 保持全屏高度。
- [x] 已使用当前 tokens/theme 重写覆盖层、排版和 CTA 状态；图片内容蒙版通过通用语义变量 `--overlay-image` 复用 `--alpha-20`。
- [x] `npm run check` 通过。
- [x] H-004 已提供 `#about-me` 个人信息区，CTA 的实际滚动目标存在。
- [x] 用户审核首屏实际视觉和代码。

#### 审查记录

- 2026-07-23：完成旧首屏主视觉结构、资源加载和响应式风险盘点，等待用户审查。
- 2026-07-23：用户确认桌面和移动端均保留全屏首屏、保留文案层级和锚点 CTA，并按新主题重设计水墨背景覆盖层；该记录只代表需求方向确认，不代表代码人工审查通过。
- 2026-07-23：完成 H-003 首屏主视觉代码实现，状态更新为“已实现 / 未审查”，等待用户审核实际文件和页面效果。
- 2026-07-23：根据用户反馈将背景图片内容蒙版提取为通用主题变量 `--overlay-image`，供首页、列表、详情和导航页复用，透明比例调整为 `--alpha-20`。
- 2026-07-23：根据暗色模式视觉反馈，为 `--overlay-image` 增加暗色主题覆盖；用户最终确认亮色使用 `--alpha-20`、暗色使用 `--alpha-80`。
- 2026-07-23：按旧项目首屏结构收敛组件，恢复“标题、签名、导语、CTA”层级和原语义类名，同时保留新项目的主题变量、装饰图空替代文本与 `100svh`。
- 2026-07-23：用户完成首屏页面与当前代码审查，确认 H-003 通过；静态检查、类型检查和生产构建均通过。
- 2026-07-24：复核 CTA 锚点，`#about-me` 已由 H-004 个人信息区提供。

### [H-004] 首页静态区块

- 实现状态：已实现
- 人工审查：已通过
- 旧项目入口：`src/components/modules/home/profile-card.tsx`、`posts-section.tsx`、`post-card.tsx`、`projects-section.tsx`、`project-card.tsx`、`src/components/site/section-heading.tsx`
- 新项目入口：`src/app/(public)/page.tsx`、`src/features/home/ui/profile-section.tsx`、`src/features/home/ui/posts-section.tsx`、`src/features/home/ui/post-card.tsx`、`src/features/home/ui/projects-section.tsx`、`src/components/site/section-heading.tsx`、`src/components/ui/button.tsx`、`src/components/ui/text-button.tsx`、`src/components/ui/tag.tsx`、`src/config/home.ts`
- 对应提交：`a285e4d`、`1d07cbe`、`b097db4`、`fa09d64`、`532895c`、`4930390`

#### 旧静态区块事实

| 区块 | 旧内容 | 静态与动态边界 |
| --- | --- | --- |
| 区块标题 | `SectionHeading` 输出 `h2`、分割线和可选操作链接 | 标题和分割线静态；“查看全部”由文章区传入 |
| 个人卡片 | 头像、作者名、签名行、简介、联系我、GitHub、RSS | 文案和链接静态；RSS 按钮有客户端复制交互 |
| 文章卡片 | 封面、标题、分类、日期、摘要和“阅读更多”视觉提示 | 结构静态，文章数据来自 H-005；整卡是单个 `/blog/[slug]` 链接 |
| 项目卡片 | 图标、项目名、描述、技术标签和外链操作 | 数据静态配置；外链操作打开新窗口 |

#### 迁移审查发现

- 旧首页将个人信息、文章和项目区块统一使用区块标题和网格布局，但卡片样式直接依赖旧主题变量，不能原样复制。
- 文章卡片使用整卡链接并避免嵌套链接，迁移时应保留这一语义结构；文章数据、封面和日期格式放到 H-005/博客单元单独审查。
- 项目卡片的技术标签和外链属于信息内容，不应只依赖颜色区分；外链需要保留明确文本和安全的 `rel` 属性。
- 旧个人卡片使用 Logo 作为头像，已按 H-001/H-003 决定改用当前独立头像资源；头像替代文本和品牌语义需在实现时确认。
- RSS 复制按钮和文章整卡链接有交互行为，键盘焦点、可访问名称和复制失败反馈不能只按静态样式迁移。
- 首页静态内容已集中在 `src/config/home.ts`；项目数据随首页配置维护即可，当前没有证据需要再抽象通用项目卡片或独立数据层。

#### 待用户确认

| 决策项 | 可选方向 | 用户决定 |
| --- | --- | --- |
| A | 保留统一区块标题 / 各区块独立标题 / 删除分割线 | 保留统一区块标题和分割线 |
| B | 保留个人卡片全部操作 / 只保留联系和 GitHub / 删除 RSS | 保留个人卡片全部操作，包括 RSS |
| C | 文章卡片整卡可点 / 仅标题可点 / 卡片与按钮分开 | 保留文章卡片整卡可点击 |
| D | 项目卡片保留两个项目 / 先保留一个 / 只保留项目列表 | 保留 `zhijian` 和 `cesium-example` 两个项目 |

#### 当前已实现

- 首页已用 `ProfileSection` 替换原 `AboutSection`，并保留 `#about-me` 锚点供首屏 CTA 使用。
- 个人信息区使用独立头像、作者名、签名、简介、邮件、GitHub 与 RSS 订阅链接；GitHub 和 RSS 外链使用 `target="_blank"`、`rel="noopener noreferrer"`。
- RSS 当前使用订阅地址跳转，不迁移旧项目的剪贴板复制交互；GitHub 热力图已确认不迁移。
- `SectionHeading` 提供 `h2`、朱砂分割线和可选右侧操作，后续最新文章与开源项目区可复用。
- `Button` 以链接语义统一个人信息区的 32px 高文本操作，并复用既有图标组件和全局焦点样式。
- 开源项目区已展示 `zhijian` 与 `cesium-example` 两张静态项目卡片；数据来自 `PROJECTS`，图标、技术标签和访问站点/GitHub 外链均使用现有基础组件。
- `Tag` 固定为 28px 高，支持普通与主色两种变体；项目标签当前采用普通变体，外链保留可读文本和 `noopener noreferrer` 防护。
- 最新文章区已复用 `SectionHeading` 与 `Tag`，桌面端三列、移动端单列；卡片以单个 `/blog/[slug]` 链接承载整卡交互，并提供“暂无文章。”空状态。
- 最新文章卡片已恢复封面、标题、分类、日期、摘要和阅读提示的旧版层级；封面仅接受站内绝对路径或 HTTPS 地址，并延迟加载且不发送来源页 Referer。

#### 验收标准

- [x] 已核对静态区块组件、卡片内容和区块标题结构。
- [x] 已区分静态展示、客户端交互和 H-005 动态数据边界。
- [x] 用户确认 A-D 四项静态区块方向。
- [x] 已迁移个人信息子步骤，并验证首屏 CTA 的 `#about-me` 目标。
- [x] 已抽取可复用区块标题和固定 32px 高的链接按钮。
- [x] 已迁移开源项目子步骤，保留两个确认项目、技术标签和安全的外链操作。
- [x] 开源项目子步骤已完成代码审查，`npm run check` 与 `git diff --check` 通过。
- [x] `npm run check` 与 `git diff --check` 通过。
- [x] 个人信息子步骤已完成代码审查，未发现阻塞提交的问题。
- [x] 已完成最新文章区的结构、整卡链接与空状态实现。
- [x] 用户完成首页静态区块整体视觉与交互验收。

#### 审查记录

- 2026-07-23：完成首页静态区块、卡片语义、外链和交互边界盘点，等待用户审查。
- 2026-07-23：用户确认保留统一标题、个人卡片全部操作、文章整卡链接和两个项目卡片。需求边界已确认，代码实现与人工审查尚未开始。
- 2026-07-24：完成个人信息子步骤并提交 `a285e4d`：接入独立头像、联系操作、可复用区块标题和 32px 链接按钮；文章与项目区仍未实现，H-004 保持“实现中 / 审查中”。
- 2026-07-24：完成开源项目子步骤并提交 `1d07cbe`：接入两个静态项目卡片、首页项目配置、固定 28px 标签组件和所需图标；最新文章区仍未实现，H-004 保持“实现中 / 审查中”。
- 2026-07-25：完成最新文章区实现：服务端文章数据传入首页、整卡链接、分类标签、日期、摘要、查看全部入口与空状态均已接入；生产构建、ESLint、类型检查和差异检查通过，等待用户进行整体视觉与交互验收。
- 2026-07-26：文字按钮、文章卡片和最新文章区已分别完成精简审查并提交；H-004 更新为“已实现 / 审查中”，仍等待首页静态区块整体视觉与交互验收。
- 2026-07-26：完成首页静态区块技术验收，修复项目标签容器无效 `aria-label`、页脚文字对比度及深色分类标签对比度；自动化扫描与浏览器复测通过，人工审查仍等待用户确认。
- 2026-07-27：用户确认 H-004 通过。

### [H-005] 首页动态数据

- 实现状态：已实现
- 人工审查：已通过
- 旧项目入口：`src/app/page.tsx`、`src/lib/domain/posts.ts`、`src/lib/domain/github.ts`、`src/components/modules/home/commit-chart.tsx`
- 新项目入口：`src/app/(public)/page.tsx`、`src/features/home/lib/page-data.ts`、`src/lib/core/db.ts`、`src/lib/core/api.ts`、`src/lib/core/errors.ts`、`src/lib/core/rate-limit.ts`、`src/lib/core/request-ip.ts`、`src/lib/domain/posts.ts`、`src/app/api/posts/latest/route.ts`、`src/types/api.ts`、`src/types/post.ts`、`docs/database/init.sql`
- 对应提交：`e7a562a`、`f47750c`、`96ccc52`、`4930390`

#### 旧动态数据事实

| 数据 | 旧实现 | 无数据或失败行为 |
| --- | --- | --- |
| 最新文章 | 服务端调用 `getPublishedPosts({ limit: 3 })`，按更新时间和发布时间倒序读取已发布文章 | 未配置数据库或查询失败时返回空数组，文章区显示“暂无文章” |
| GitHub 提交 | 服务端调用 `fetchCommitHistory()`，读取仓库最近 180 天提交并按上海时区按日聚合 | 未配置 `GITHUB_TOKEN` 时返回空数组，热力图区显示配置提示 |
| 首页编排 | `Promise.all` 并行请求文章和 GitHub 数据，避免两个独立请求串行等待 | 任一 Promise 未处理的异常都可能影响整页渲染，需要在新实现确认错误边界 |
| GitHub 缓存 | `unstable_cache` 缓存提交历史 1 小时，最多翻页 20 次，每页 100 条 | 非 2xx 响应直接停止；网络异常未在旧函数内统一降级 |

#### 迁移审查发现

- 新项目只迁移最新文章数据，并将查询保留在 Server Component 数据边界；客户端只接收最小展示数据。
- 文章空状态是合法产品状态，不能用占位文章或硬编码示例数据掩盖数据库未配置。
- 首页已确定采用 60 秒 ISR，文章查询使用同周期服务端数据缓存，不照搬旧页面的 `force-dynamic`。
- 文章查询结果和空状态文案在服务端完成，避免首屏先渲染空壳再由客户端二次请求。
- GitHub 提交热力图不迁移，因此不引入旧 `fetchCommitHistory()`、`GITHUB_TOKEN`、交互组件或一小时缓存。

#### 待用户确认

| 决策项 | 可选方向 | 用户决定 |
| --- | --- | --- |
| A | 首页保留最近 3 篇 / 调整数量 / 暂时无文章区 | 首页保留最近 3 篇文章 |
| B | GitHub 热力图保留为可选增强 / 必须配置 token 才显示首页 / 删除 | 删除，不迁移 |

#### 验收标准

- [x] 已核对文章查询、缓存周期和首页服务端数据边界。
- [x] 已记录无数据库、查询失败和空状态行为。
- [x] 用户确认保留最近 3 篇文章，并取消 GitHub 提交热力图。
- [x] 已实现仅查询三篇已发布文章的 MySQL 数据层，并由首页 Server Component 直接调用。
- [x] 已提供 `/api/posts/latest` 作为同一查询的公开封装，不在首页发起内部 HTTP 请求。
- [x] 已提供 60 秒文章缓存、每进程接口限流和有上限的数据库连接池等待队列，降低数据库被高频请求直接耗尽的风险。
- [x] 已验证数据库不可用时首页维持空状态，接口返回不含内部信息的 `503` JSON 响应。
- [x] 首页已改为 60 秒 ISR，避免不同客户端在缓存周期内重复触发数据库查询。
- [x] 已统一 API 成功/错误响应为 `{ code, message, data }`，并集中校验代理传入的客户端 IP。
- [x] 已迁移数据库初始化脚本至 `docs/database/init.sql`，补充最新文章查询复合索引与已有表升级说明。
- [x] 已确认不迁移 GitHub 提交热力图，不新增对应 Token、接口、缓存或客户端状态。
- [ ] 在 Nginx 部署配置中补充 HTTPS、响应安全头和跨实例限流。

#### 审查记录

- 2026-07-23：完成首页文章与 GitHub 数据入口、缓存、失败降级和服务端边界盘点，等待用户审查。
- 2026-07-23：用户确认保留最近 3 篇文章、可选 GitHub 热力图、独立失败降级、1 小时缓存和并行服务端读取。需求边界已确认，代码实现与人工审查尚未开始。
- 2026-07-25：完成最新文章查询与公开接口。本地验证期间出现过数据库连接超时，首页与接口均以 HTTP 200 和空数组降级；连接恢复后接口已实际返回三篇文章，首页最新文章区正常渲染，待用户完成视觉与交互验收。
- 2026-07-25：按服务器与数据库安全审查调整文章数据层：数据库不可用时首页空状态不变，公开接口改为 `503`；新增 60 秒缓存、进程内限流、受限连接池队列、`server-only` 保护和封面 URL 协议校验。Nginx 的 HTTPS、安全头与跨实例限流仍待部署时配置。
- 2026-07-26：将首页由按请求动态渲染调整为 60 秒 ISR，收窄数据库异常捕获并保留脱敏错误码；从原项目迁移数据库初始化脚本，补充与最新文章排序对应的复合索引。
- 2026-07-26：统一 API 成功与错误 JSON 结构为 `{ code, message, data }`，提取基于 Node.js 标准库校验的客户端 IP 方法；ESLint、TypeScript、生产构建与 `503 no-store` 运行时响应验证通过。
- 2026-07-26：用户明确取消 GitHub 提交热力图，H-005 以最新文章数据链路完成为边界，更新为“已实现 / 审查中”。
- 2026-07-27：用户确认 H-005 通过；Nginx 的 HTTPS、响应安全头和跨实例限流仍作为部署阶段边界保留。

### [H-006] 首页整体验收

- 实现状态：已实现
- 人工审查：已通过
- 验收入口：`src/app/(public)/page.tsx`、`src/app/(public)/layout.tsx` 及首页、站点、基础 UI 组件
- 对应提交：`f812638`、`40a9f08`

#### 技术验收结果

- 使用生产构建在 `1440x900` 浅色/深色、`390x844` 和最小 `320x568` 浅色视口完成浏览器检查；无横向溢出、控制台错误或资源加载失败。
- 页面保持单个 `main`、连续的 `h1`/`h2`/`h3` 层级、`zh-CN` 文档语言、区块可访问名称和安全外链属性。
- 桌面端前 10 个可交互元素的 Tab 顺序符合视觉顺序，均显示全局 2px 焦点轮廓；首屏 CTA 跳转后目标区块位于固定页头下方。
- Axe 按 WCAG 2 A/AA、2.1 AA 和 2.2 AA 标签扫描浅色与深色首页，均为 0 违规、0 待人工判定；修复前发现的页脚和深色标签对比度问题已复测通过。
- ESLint、Next 路由类型生成、TypeScript 严格检查和生产构建通过；首页继续输出 60 秒 ISR，`/api/posts/latest` 保持 60 秒共享缓存。
- 本地生产模式观测到 FCP 约 84ms，仅用于发现明显回归，不替代部署环境的 Lighthouse 与真实网络 Core Web Vitals。

#### 已知边界

- `/blog` 仍是占位页，首页文章卡片对应的 `/blog/[slug]` 当前返回 404；该行为由 `BL-001`、`BL-002` 后续单元处理，不在本次首页验收中伪造详情路由。
- 本次未执行 NVDA 等真实屏幕阅读器人工测试，也未在生产域名、Nginx 和真实移动设备上验证；这些验证保留为部署阶段质量边界。
- 生产依赖审计中的 3 个高危风险仍按既有决定保留为独立遗留项。

#### 验收标准

- [x] 完成桌面、移动、窄屏、浅色与深色浏览器检查。
- [x] 完成键盘顺序、焦点可见性、锚点偏移与内部路由状态检查。
- [x] 完成 WCAG 自动扫描并修复本次发现的问题。
- [x] 完成 ESLint、TypeScript 和生产构建验证。
- [x] 用户确认首页整体视觉与交互结果。

#### 审查记录

- 2026-07-26：完成首页技术验收并修复 3 项验收问题；H-006 更新为“已实现 / 审查中”，等待用户确认整体视觉与交互结果。
- 2026-07-27：用户确认 H-006 通过。

### [C-002] 元数据基础

- 实现状态：已实现
- 人工审查：审查中
- 旧项目入口：`src/lib/core/site.ts`、`src/lib/core/metadata.ts`、`src/app/page.tsx`
- 新项目入口：`src/config/metadata.ts`、`src/features/home/lib/metadata.ts`、`src/features/blog/lib/metadata.ts`、`src/app/layout.tsx`、`src/app/(public)/page.tsx`、`src/app/(public)/blog/page.tsx`
- 对应提交：`5464b81`、`8e7cf29`、`4930390`

#### 当前已实现

- `src/config/metadata.ts` 集中维护站点 URL、品牌标题、描述、关键词与根布局默认元数据；首页与博客的页面专属 metadata 分别位于对应 feature 的 `lib/metadata.ts`，不迁移旧项目混合导航、路由和存储键的全局配置文件。
- 服务端读取并校验 `SITE_URL`；未配置时回退正式域名 `https://yuwb.dev`，非 HTTP(S)、携带认证信息或不是站点根地址时直接拒绝构建。
- 根布局提供标题模板、作者、favicon、默认 Open Graph、Twitter Card 和索引策略；首页提供绝对标题、canonical、完整 OG/Twitter 内容和基于当前首页截图生成的默认分享图。
- 默认分享图使用 `public/images/og-default.webp`，规格为 1200 × 630、38,348 字节；顶部对齐裁切保留导航、品牌标题、首屏文案和主要操作区域。
- 首页服务端 HTML 注入仅包含 `WebSite` 与 `Person` 的 JSON-LD；序列化时转义 `<`，避免内容提前闭合 `script` 标签。
- `/blog` 已输出正式分页列表元数据；数据库不可用时临时输出 `noindex, nofollow, noarchive`，避免搜索引擎收录故障状态。
- 当前不迁移旧项目的 RSS alternate、manifest、Sitemap、文章 `ItemList` 或搜索动作，避免声明尚不存在的页面和能力。

#### 验收标准

- [x] 首页输出正式 title、description、canonical、Open Graph 和 Twitter Card。
- [x] 首页 JSON-LD 可解析为 `WebSite` 与 `Person`，并只引用当前存在的站点、头像和 GitHub 地址。
- [x] 博客列表输出正式 title、description、canonical、Open Graph 和 Twitter Card；数据库不可用时禁止索引。
- [x] ESLint、Next 路由类型生成、TypeScript 严格检查和生产构建通过。
- [x] 生产模式 HTML 已验证 canonical、OG 图片使用 `https://yuwb.dev` 绝对地址。
- [ ] 部署环境显式配置 `SITE_URL` 并复核最终生产域名。
- [ ] 用户确认首页搜索摘要与分享卡片文案。

#### 审查记录

- 2026-07-26：从旧项目迁移并精简首页 SEO 基础，完成站点 URL 校验、正式元数据、首页 JSON-LD、博客占位页 `noindex` 和新版 OG 分享图；代码审查未发现阻断问题，ESLint、TypeScript、生产构建和差异检查均通过，对应提交为 `5464b81`；C-002 更新为“已实现 / 审查中”，等待用户确认最终搜索摘要与分享卡片文案。
- 2026-07-27：`BL-001` 接入正式博客列表后，`/blog` 已改为输出分页 title、description、canonical、Open Graph 与 Twitter Card；分享文案继续复用当前站点描述，仍等待用户确认最终措辞。
- 2026-07-27：首页专属 metadata 与 JSON-LD 移入 `src/features/home/lib/metadata.ts`；站点级配置仍保留在 `src/config/metadata.ts`，输出内容不变。

### [D-002] 文章只读数据层

- 实现状态：已实现
- 人工审查：已通过
- 旧项目入口：`src/lib/domain/posts.ts`、`src/lib/domain/post-shared.ts`
- 新项目入口：`src/lib/domain/posts.ts`、`src/types/post.ts`
- 对应提交：`9c9fb6c`

#### 当前已实现

- `getPublishedPostsPage()` 仅查询已发布文章的列表展示字段，不读取正文；结果包含文章预览、规范化后的页码和页大小、总数与总页数。
- 默认页大小为 10；页码限制为 1 至 1,000，每页条数限制为 1 至 100，避免异常参数造成无界 `OFFSET` 或大结果集查询。
- 支持单选 `categorySlug` 和最多 20 个 `tagSlugs`；分类与标签组取交集，多个标签沿用旧站的任意命中语义，所有条件均使用参数化查询。
- 总数和当前页列表使用参数化 SQL 并行查询；列表按更新时间、发布时间和 ID 倒序，匹配现有首页最新文章的排序规则与复合索引。
- 空列表是合法结果，返回空文章数组和 `total: 0`；未配置数据库或任一查询失败时抛出 `ServiceUnavailableError`，由页面或 API 按各自的用户反馈边界降级，不把故障伪装成空数据。
- 每种分页和筛选组合通过 60 秒服务端缓存独立缓存；首页固定三篇查询和 `/api/posts/latest` 保持原有行为不变。

#### 验收标准

- [x] 已提供受限分页、总数和总页数的已发布文章只读查询。
- [x] 已使用参数化 SQL，列表查询不读取 Markdown 正文，列表与总数并行执行。
- [x] 已区分正常空数据与数据库不可用，并复用现有脱敏服务错误类型。
- [x] 已保留首页最新文章查询和现有公开 API 的行为边界。
- [x] 用户审查分页参数、排序、空数据与故障边界。

#### 审查记录

- 2026-07-27：完成 D-002 分页文章只读数据层实现；ESLint、路由类型生成、严格 TypeScript、生产构建和差异检查通过。当前未在独立数据库环境执行分页查询，博客页面与公开分页 API 仍由后续 `BL-001`、`BL-004` 单元接入，等待用户审查。
- 2026-07-27：用户确认继续下一步，D-002 标记为已通过。

### [BL-001] 博客列表

- 实现状态：已实现
- 人工审查：已通过
- 旧项目入口：`src/app/blog/page.tsx`、`src/components/modules/blog/list-client.tsx`、`src/components/modules/blog/post-item.tsx`
- 新项目入口：`src/app/(public)/blog/page.tsx`、`src/app/(public)/blog/page.module.css`、`src/features/blog/ui/post-list.tsx`、`src/features/blog/ui/post-list.module.css`、`src/features/blog/ui/blog-filters.tsx`、`src/features/blog/ui/blog-filters.module.css`、`src/features/blog/lib/metadata.ts`、`src/components/ui/pagination.tsx`、`public/images/bg-ink.webp`
- 对应提交：`fd3bef9`、`eb710e5`、`8e7cf29`

#### 当前已实现

- `/blog` 使用 Server Component 读取 URL 中的 `page`、`category` 与 `tags` 参数并直接调用 D-002；不通过内部 HTTP 请求。移动端筛选组件仅管理原生 `dialog` 开关，不在客户端请求或筛选文章数据。
- 页面恢复旧站列表专用水墨背景、单一“文章”页头和紧凑阅读列表；移除首版额外添加的站点说明、文章总数与“阅读更多”提示。
- 列表保留旧站“正文在左、固定宽度可选封面在右、整行可点击”的阅读结构；移动端封面置顶，分类使用主色文字，日期靠右，并继续复用服务端封面 URL 白名单。
- 分类使用单选链接，标签使用可叠加链接；切换筛选时重置为第 1 页，标签参数使用 `tags=slug-a,slug-b`，分页继续保留当前筛选。桌面端筛选置于列表右侧，移动端通过原生 `dialog` 展示；当前筛选显示在列表顶部，支持逐项移除和“全部清除”。
- 第一页使用 `/blog` 规范 URL，后续页使用 `/blog?page=N`；通用分页组件复用 32px `Button` 的默认、当前页与禁用状态，并提供“上一页、页码、省略号、下一页”结构，博客页面只负责生成目标 URL；超出总页数的访问重定向到最后一页，空文章库显示明确空状态。
- 数据库不可用时页面显示独立的服务不可用状态并禁止索引；数据可用时输出正式 title、description、canonical、Open Graph 与 Twitter Card。
- 筛选结果页输出对应的 title、description、canonical、Open Graph 与 Twitter Card，并设置 `noindex, follow`；文章详情仍保留给后续单元，文章链接会在 `BL-002` 前继续指向尚未实现的详情路由。

#### 验收标准

- [x] 已将 `/blog` 占位页替换为服务端分页文章列表。
- [x] 已实现页码式分页、超界重定向、空状态和数据库不可用状态。
- [x] 已复用当前主题覆盖层、旧站列表水墨背景、文章日期、分类文字和安全封面加载规则。
- [x] 已实现分类单选、标签多选、清除筛选、筛选分页保持与筛选结果的搜索索引策略。
- [x] 已移除博客占位页 `noindex`，并添加正式列表元数据。
- [x] 用户完成博客列表桌面、移动端、分页和状态页面验收。

#### 审查记录

- 2026-07-27：完成 BL-001 初版实现；ESLint、路由类型生成、严格 TypeScript、生产构建和差异检查通过，等待用户审查。未迁移分类/标签筛选、详情页和公开分页 API。
- 2026-07-27：用户退回首版视觉，指出列表页与旧项目差距过大；页面已按旧站的背景、页头、文章行和页码式分页重新实现，分类/标签筛选仍按既定边界留作后续独立步骤，等待再次审查。
- 2026-07-27：迁移分类和标签筛选；`/blog` 与 `/api/posts` 统一支持 `category`、`tags`，实际文章库验证了分类、标签和组合筛选。等待用户审核筛选布局与交互。
- 2026-07-27：`fd3bef9`、`eb710e5`、`8e7cf29` 完成文章列表、筛选与页面集成；补充 alpha 令牌并修复筛选对话框在视口变化时的关闭行为。ESLint、路由类型生成、严格 TypeScript、生产构建和差异检查通过，仍等待用户完成页面视觉与交互审查。
- 2026-07-27：用户确认 BL-001 通过。

### [BL-002] 文章详情

- 实现状态：已实现
- 人工审查：未审查
- 旧项目入口：`src/app/blog/[slug]/page.tsx`、`src/components/site/article-view.tsx`、`src/components/site/article-header.tsx`
- 新项目入口：`src/app/(public)/blog/[slug]/page.tsx`、`src/features/blog/lib/detail-page-data.ts`、`src/features/blog/ui/post-detail.tsx`、`src/features/blog/ui/article-content.tsx`、`src/lib/domain/posts.ts`
- 对应提交：`52bf772`、`f2aa97e`、`5b92ed2`、`155de84`

#### 当前已实现

- `/blog/[slug]` 使用服务端动态路由读取已发布文章详情；非法、草稿和不存在的 slug 统一渲染 404 界面并禁止搜索引擎收录。受 App Router 流式响应限制，动态请求的 HTTP 状态可能保持 200。
- 详情查询仅接受既有 slug 规则，读取正文、分类、标签、封面与时间；封面地址继续限制为站内绝对路径或 HTTPS，标签 JSON 限制为最多 20 个有效正整数。
- 页面顶部输出“首页 / 文章 / 当前文章”面包屑，随后展示无边框、自然比例封面、标题、摘要、分类、标签、发布时间和更新时间；列表页与详情页继续共用水墨背景和主题令牌。
- `ArticleContent` 独立封装封面和正文展示，后续后台编辑预览直接复用；正文安全渲染由 `BL-003` 的 Markdown 组件统一提供。
- 详情页生成 canonical、文章 Open Graph、Twitter Card 和 `BlogPosting` JSON-LD；数据库 DATETIME 按中国时区转换为标准 ISO 时间，JSON-LD 中的封面地址为绝对 URL。

#### 验收标准

- [x] 已实现已发布文章详情查询、非法 slug 收敛、404 界面与服务不可用状态。
- [x] 已提供可由后台预览复用的封面与正文展示组件。
- [x] 已输出详情页的 canonical、文章社交元数据和安全 JSON-LD。
- [x] 本次新增和修改的博客文件已通过 ESLint、差异格式检查、全量 TypeScript 与生产构建检查。
- [ ] 用户完成桌面与移动端详情页、封面、时间、404 和服务不可用状态审查。

#### 审查记录

- 2026-07-27：完成 BL-002 初版实现。全量类型检查当前被另一个对话正在修改的导航页阻断；本单元的定向 ESLint、严格 TypeScript 与差异检查均已通过，等待用户审查。
- 2026-07-28：复核动态流式路由的缺失文章响应：页面输出 404 界面与 `noindex`，HTTP 状态保持 200 属于 App Router 流式响应限制；封面保持自然比例。ESLint、全量 TypeScript 与生产构建通过。
- 2026-07-28：`52bf772`、`f2aa97e`、`5b92ed2`、`155de84` 分别完成详情读取、元数据、展示组件与路由接入；ESLint、路由类型生成、严格 TypeScript 和生产构建通过，等待用户完成详情页验收。

### [BL-003] Markdown 渲染

- 实现状态：已实现
- 人工审查：未审查
- 旧项目入口：`src/components/site/markdown-article.tsx`、`src/components/site/markdown-article.module.css`、`src/components/site/code-block.tsx`
- 新项目入口：`src/features/blog/ui/markdown-article.tsx`、`src/features/blog/ui/markdown-article.module.css`、`src/features/blog/ui/article-content.tsx`
- 对应提交：`017db20`

#### 当前已实现

- 使用 `react-markdown`、`remark-gfm` 与 `rehype-highlight` 渲染 Markdown、GFM 表格/任务列表和声明语言的代码块。
- 不使用 `rehype-raw` 或任何原始 HTML 解析插件；Markdown 中的 HTML 不会作为页面 DOM 执行或注入。
- HTTP(S) 外链自动以新标签页打开，并带 `noopener noreferrer`；相对链接与其他已由渲染器允许的安全协议不强制改写。
- 正文图片延迟加载，上传图片和外链不走 `next/image`；封面和正文继续由 `ArticleContent` 统一组合，以供详情页和后续后台预览复用。
- 文章正文恢复标题、段落、引用、列表、行内代码、代码块、图片、表格、分隔线与深色主题排版；正文 Markdown 标题整体下移一级，避免与页面标题重复并保持内容层级。

#### 验收标准

- [x] 已完成安全 Markdown、GFM 与代码高亮的渲染边界。
- [x] 已完成外链、新窗口安全属性、图片懒加载和正文标题层级处理。
- [x] 已按现有水墨背景、宣纸阅读面和亮暗主题完成详情页阅读排版。
- [x] 本次新增和修改的 Markdown 文件已通过 ESLint、差异格式检查、全量 TypeScript 与生产构建检查。
- [ ] 用户完成长文、代码块、表格、图片、外链、浅色与深色模式的视觉与交互审查。

#### 审查记录

- 2026-07-27：根据用户反馈完成详情页 Markdown 排版和阅读层级重构。全量类型检查当前被另一个对话正在修改的导航页阻断；本单元的定向 ESLint、严格 TypeScript、差异检查与真实文章路由请求均已通过，等待用户审查。
- 2026-07-28：修正 Markdown 标题降级后与原有 h2 同级的问题；ESLint、全量 TypeScript 与生产构建通过。
- 2026-07-28：`017db20` 完成 Markdown 正文渲染与代码复制；触屏设备常显复制操作，直接依赖仅保留渲染、GFM 与高亮插件。ESLint、路由类型生成、严格 TypeScript 和生产构建通过，等待用户完成内容与交互验收。

### [BL-004] 公开内容接口

- 实现状态：已实现
- 人工审查：已通过
- 旧项目入口：`src/app/api/posts/route.ts`
- 新项目入口：`src/app/api/posts/route.ts`、`src/lib/domain/posts.ts`、`src/types/post.ts`
- 对应提交：`dfa0e7b`

#### 当前已实现

- `GET /api/posts` 接受可选的 `page`、`pageSize`、`category` 与逗号分隔的 `tags` 查询参数，分页返回已发布文章预览，不包含 Markdown 正文。
- 缺省或非法参数回退默认值；页码限制为 1 至 1,000，每页条数限制为 1 至 100，标签最多 20 个，实际规范化结果随响应返回。
- 路由复用 `getPublishedPostsPage()`，不重复文章 SQL；`/blog` 继续由 Server Component 直接调用领域层，不绕行内部 HTTP 接口。
- 成功响应沿用 `{ code, message, data }` 契约与 60 秒共享缓存，数据库不可用时返回脱敏的 `503`，限流时返回带 `Retry-After` 的 `429`。
- `/api/posts/latest` 继续只返回首页需要的三篇最新文章，不接受分页参数。

#### 验收标准

- [x] 已提供职责独立的公开分页接口，并保留固定最新文章接口。
- [x] 已限制分页范围，列表响应不包含正文。
- [x] 已支持与博客页面一致的分类和标签筛选条件。
- [x] 已复用统一响应、错误处理、缓存、客户端 IP 提取与公开接口限流。
- [x] 博客服务端页面未改为请求站内 API。
- [x] 用户审查公开接口的参数、响应与职责边界。

#### 审查记录

- 2026-07-27：完成 `/api/posts` 公开分页接口；ESLint、路由类型生成、严格 TypeScript、生产构建和差异检查通过。本地接口验证了统一响应、缓存头、默认参数与上限收敛，等待用户审查。
- 2026-07-27：新增 `category` 和 `tags` 筛选参数；实际文章库验证分类、单标签和组合筛选均正常，等待用户审查。
- 2026-07-27：用户确认 BL-004 通过。

## 11. 下一步

H-001 至 H-006、BL-001 与 BL-004 已全部通过人工审查；GitHub 提交热力图已从迁移范围删除。首页迁移单元、博客列表、公开分页接口与文章详情已完成实现；当前等待单独审查：

1. 用户确认 `BL-002` 与 `BL-003` 的详情页视觉、状态、Markdown 内容和元数据输出；
2. `C-002` 的首页搜索摘要与分享卡片文案继续等待用户确认；
3. 博客详情通过后，再审查 RSS、Sitemap 与 Robots 的迁移范围。
