/*============================================================================
  api - 公开接口响应类型

  所有公开 API 使用 code、message、data 同一结构，供服务端与调用方共享。
============================================================================*/

export interface ApiResponse<TData> {
    code: string;
    message: string;
    data: TData;
}
